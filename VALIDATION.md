# KeyMate 0.1.0 검증

검증일: 2026-09-23 (한국 표준시). Windows 11 x64, OS 빌드 26200, .NET SDK 8.0.425 / 런타임 8.0.31에서 확인했습니다.

| 구분 | 결과 |
| --- | --- |
| Release 솔루션 빌드 | 경고 0, 오류 0 |
| 핵심·SQLite·JSON 테스트 | 19/19 통과 |
| Windows 입력 통합 테스트 | 19/19 통과 |
| 자체 포함 단일 EXE | 생성 성공, 실행 종료 코드 0 |
| WPF 화면 확인 | 항목 목록·편집 대화상자·다크 설정 화면 렌더링 후 시각 확인 |

## 자동 검사 내용

핵심 테스트: 한글과 영문 매칭, 단어 경계, 결합 문자 앞 경계, 비활성 항목, 가장 긴 단축어 우선, 대소문자, 날짜·시간, 정확한 앱 제외, 중복·제어 문자 검증, 초기화 1회, Unicode·여러 줄·SQL 특수문자 저장, 항목 수정, 설정 재로딩, JSON 왕복, 잘못된 백업의 전체 취소, 백업 ID 충돌 방지, 마지막 항목 삭제 후 재생성 방지.

입력 테스트: 실제 WPF 입력창에서 확정된 한글/영문 + Space, 오탐 경계, 여러 줄 Unicode, 일시 정지, 후속 입력이 빠를 때 취소, 비밀번호, 읽기 전용, 앱 제외, 비활성 항목, 영문 Enter/Tab 성공 시 키 소비, 불일치 Enter/Tab 원래 동작, Microsoft 한국어 IME 실제 `w,t → ㅈㅅ` 및 `d,k,s,s,u,d → 안녕` 조합 후 Space 치환, 한글 Enter 미치환, 별도 프로세스의 편집창에서 한글·여러 줄 치환.

실제 키 이벤트는 테스트용 표식을 붙여 `SendInput`으로 보냈고, 이 표식을 받는 진단 모드는 테스트 프로그램에서만 켰습니다. 제품 앱은 자신의 주입 입력과 다른 합성 입력으로 다시 치환하지 않습니다. 모든 키 입력 전 테스트 창이 전경인지 검사합니다.

## 아직 검증하지 않은 범위

- Chrome, 카카오톡, VS Code, Word의 개별 버전 및 입력창별 호환성
- 관리자 권한 앱, 게임, 터미널, 원격 데스크톱, 비표준 접근성 공급자
- Windows 10, ARM64, 다중 모니터 DPI 전환, 매우 큰 문서·장시간 실행
- 실제 재부팅 후 자동 시작, 배포 코드 서명·SmartScreen 평판

현재 결과는 모든 Windows 앱에서의 호환성을 의미하지 않습니다. 전체 제약과 사용 방법은 [README](README.md)를 확인하세요. 통합 테스트는 대화형 Windows 데스크톱이 필요하므로 GitHub Actions에서는 빌드·핵심 테스트·배포 파일 생성만 실행합니다.
